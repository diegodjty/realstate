import React from 'react';
import Layout from './../components/Layout';
const properties = () => {
    return (
        <Layout>
            <h1>Properties</h1>
        </Layout>
    );
};

export default properties;